import React, { useState } from "react";
import { Card, Col, Container, Button, Image, Row } from "react-bootstrap";
import "./Customer.css";

const Customer = () => {
  const [data, setData] = useState([]);

  return (
    <div>
      <h1>Hello world</h1>
    </div>
  );
};

export default Customer;
